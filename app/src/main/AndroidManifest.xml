<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ect.inventaireect">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ect"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ect"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".first"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FPG" />
        <activity android:name=".EDBFP" />
        <activity android:name=".chaie" />
        <activity android:name=".autredb" />
        <activity android:name=".matinf" />
        <activity android:name=".autremi" />
        <activity android:name=".AAI" />
        <activity android:name=".delete"></activity>
    </application>

</manifest>